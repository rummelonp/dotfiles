#!/usr/bin/env ruby

class String
  GLITCH_TABLE = ("\u0300".."\u036f").to_a

  def glitch
    each_char.map { |c|
      c + rand(2).times.map { GLITCH_TABLE.sample }.join
    }.join
  end
end

if $0 == __FILE__
  puts ARGV.join(' ').glitch
end
